
<!DOCTYPE html>
<html lang="fr">

<head>
    <title>Modifier Profil</title>
    <link href="/bs/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <%- include("Navbar.component.ejs",{ page: "Mon compte" }) %>
        <div class="container">
            <span class="h4 ml-2">Modifier les informations du compte</span>
            <form class="mt-4" method="POST" action="/modifier-mon-compte">
                <% if(twitter_subscribe == "undefined") { %>
                <div class="form-group">
                        <label for="Input1">Email</label>
                        <input type="text" value="<%= user.email %>" class="form-control <%= error.email != '' ? 'is-invalid' : '' %>" id="Input1" name="email">
                        <small class="text-danger">
                            <%= error.email %>
                        </small>
                </div>
                <div class="form-group">
                    <label for="pseudo">Pseudo</label>
                    <input type="text" value="<%= user.pseudo %>" class="form-control <%= error.pseudo != '' ? 'is-invalid' : '' %>" id="pseudo" name="pseudo">
                    <small class="text-danger">
                        <%= error.pseudo %>
                    </small>
                </div>
                <div class="form-group">
                    <label for="pseudo_twitter">Pseudo Twitter</label>
                    <input type="text" value="<%= user.pseudo_twitter %>" class="form-control <%= error.pseudo_twitter != '' ? 'is-invalid' : '' %>" id="pseudo_twitter" name="pseudo_twitter">
                    <small class="text-danger">
                        <%= error.pseudo_twitter %>
                    </small>
                </div>
                <div class="form-group">
                    <label for="mdp">Ancien mot de passe</label>
                    <input type="password" class="form-control <%= error.old_password != '' ? 'is-invalid' : '' %>" id="mdp" name="old_password" placeholder="Saisissez votre ancien mot de passe">
                    <small class="text-danger">
                        <%= error.old_password %>
                    </small>
                </div>
                <div class="form-group">
                    <label for="mdp">Nouveau mot de passe</label>
                    <input type="password" class="form-control <%= error.password != '' ? 'is-invalid' : '' %>" id="mdp" name="password" placeholder="Saisissez votre nouveau mot de passe">
                    <small class="text-danger">
                        <%= error.password %>
                    </small>
                </div>
                <div class="form-group">
                    <label for="mdp">Confirmation nouveau mot de passe</label>
                    <input type="password" class="form-control <%= error.conf_password != '' ? 'is-invalid' : '' %>" id="mdp" name="conf_password" placeholder="Confirmer votre nouveau mot de passe">
                    <small class="text-danger">
                        <%= error.conf_password %>
                    </small>
                </div>
                <% } else { %>
                    <div class="form-group">
                            <label for="Input1">Email</label>
                            <input type="text" value="<%= currentUser.email %>" class="form-control <%= error.email != '' ? 'is-invalid' : '' %>" id="Input1" name="email">
                            <small class="text-danger">
                                <%= error.email %>
                            </small>
                    </div>
                    <p>Vous vous êtes connecté à la plateforme avec votre compte Twitter.</p>
                    <p>Si vous souhaitez modifier davantage de vos informations personnelles, veuillez accéder <a href="https://twitter.com/settings/account">aux paramètres de votre compte Twitter.</a></p>
                <% } %>
                <button type="submit" class="btn btn-primary">Enregistrer</button>
                <a href="/mon-compte" class="btn btn-outline-secondary">Annuler</a>
            </form>
        </div>

        <script src="/js/jquery.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>
</body>

</html>