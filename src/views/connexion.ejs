<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Page de connexion</title>
    <link href="/bs/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-4">
        <div class="row d-flex justify-content-center">
            <div class="col-6">
                
                <% if(typeof(erreurs) !== "undefined") {%>
                    <div class="alert alert-dismissible alert-danger mb-4">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                    <ul>
                        <% erreurs.forEach(function(erreur){ %>
                        <li class="text-danger"><%= erreur %></li>
                        <% });%>
                    </ul>
                    </div>
                <%}%>
                <% if(typeof(success) !== "undefined" && success) { %>
                    <div class="alert alert-dismissible alert-success">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        Merci pour votre inscription ! <br>
                        Connectez-vous pour utiliser nos services !
                    </div>
                <% } %>

                <% if(typeof(delete_account) !== "undefined" && delete_account) { %>
                    <div class="alert alert-dismissible alert-success">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        Votre compte a bien été supprimé.<br>
                        Nous espérons que vous avez apprécié d'avoir utilisé nos services !
                    </div>
                <% } %>
                <h1>Connexion</h1>
                <hr>
                <form method="POST" action="/user-connexion">
                    <div class="form-group">
                        <label for="Input1">Email ou pseudo</label>
                        <input type="text" class="form-control" id="Input1" name="identifiant"
                            placeholder="Saisissez votre email ou pseudo">
                    </div>
                    <div class="form-group">
                        <label for="InputPassword">Mot de passe</label>
                        <input type="password" class="form-control" id="InputPassword" name="password"
                            placeholder="Saisissez votre mot de passe">
                    </div>
                    <button type="submit" class="btn btn-primary">Se connecter</button>
                    <a href="/form-sign" class="btn btn-outline-secondary">S'inscrire</a>
                </form>

            </div>
        </div>
    </div>
    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
</body>

</html>